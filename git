git init (to initialize empty repository)

git status 

git config --global user.name "karunakar"

git config --global user.email "spkreddy.qa@gmail.com

git config --list -- it will display the list.

git config --global --unset user.name

git config --global --unset user.email

git config --list -- to check the list